# globasoft-countries
Countries Data App est une mini application interne permettant de récupérer des données relatives aux pays à partir d’une API externe unique, de les stocker dans une base de données locale, puis de les exploiter, afficher et analyser via une application web développée avec Django.
